# Terraform files
*.tfstate
*.tfstate.*
#*.tfvars
.terraform/
.terraform.lock.hcl

# Crash log files
crash.log

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore generated Ansible inventory
ansible/inventory/hosts.ini

# Ignore generated Ansible configuration
ansible/ansible.cfg

# Ignore Ansible vault secrets
ansible/vault-secrets/*

# Plex credentials (contains passwords)
!ansible/inventory/group_vars/all/plex.yml
!ansible/inventory/group_vars/all/plex_vault.yml

# Ignore terraform.json
terraform.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# SSH Keys
ssh_keys/*
!ssh_keys/.gitkeep

# Credentials
ansible/credentials/*
!ansible/credentials/.gitkeep

# Outputs folder
outputs/*
outputs/logs/*

ansible/inventory/files/bedrock-backup.gz